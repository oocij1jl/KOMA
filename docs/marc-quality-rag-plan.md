# MARC 품질 / RAG 개선: 현행 분석 및 작업 순서

## 1. 이번 단계의 범위와 브랜치

이번 단계는 **코드 분석, 기존 결과 확인, 필드 분류, 개선 순서 설계, 기준선 고정**이다. 생성 로직·RAG·프런트 코드는 아직 변경하지 않았다.

브랜치는 추측이 아니라 실제 tree·log·diff 비교로 확정했다.

| 확인 | 명령 | 결과 |
|---|---|---|
| 커밋 동일성 | `git rev-parse origin/develop origin/feature/pipeline-v3` | 둘 다 `9e173e9`. 현재 작업 브랜치 HEAD도 같다 |
| 구현 규모 차이 | `git diff --stat origin/main..origin/develop` | 132개 파일, +26949 / -3 |
| 이력 구조 | `git log --all --graph --decorate --oneline` | `main`에는 초기 구조 2커밋만. 백엔드·RAG·평가는 모두 develop 계열 병합 |
| 중간발표 자료 | `git log -1 -- mid_result` | `27d89c3` `test: 중간발표회 도서 평가 데이터 추가 및 평가 지표 업데이트` |

- 분석·개선 기준: **`origin/develop` = `9e173e9`**. 같은 커밋인 `feature/pipeline-v3`를 따로 기준으로 잡을 이유는 없다.
- `main`은 초기 디렉터리·샘플만 있어 기준으로 쓸 수 없다.
- 작업 브랜치: **`feature/marc-quality-rag`**, `origin/develop`에서 생성 완료.
- 중간발표 자료 커밋 이후 develop에는 다건 처리·내보내기 등 변경이 더 있다.
- 따라서 **중간발표 저장 결과**와 **현재 코드로 새로 생성한 결과**를 같은 것으로 취급하지 않는다.
- 기존 `mid_result/`는 수정하지 않고 비교 기준으로 보존한다. 1단계 산출물은 이 브랜치에 로컬 커밋했고, 원격 push와 PR은 아직 하지 않았다.

브랜치 생성에 사용한 명령:

```sh
git switch -c feature/marc-quality-rag origin/develop
```

현재 이미 이 브랜치에 있으므로 다시 생성하지 않는다. 작업 커밋을 만든 뒤 원격에 처음 올릴 때는 아래처럼 대상 브랜치를 명시한다. 현재 upstream은 생성 기준인 `origin/develop`이므로 무심코 develop으로 push하지 않는다.

```sh
git push -u origin feature/marc-quality-rag
```

PR 대상은 `develop`으로 한다. 이번 작업에 관계없는 기존 변경을 함께 커밋하지 않는다.

## 2. 현재 코드 구조와 실제 생성 방식

### 2.1 백엔드 흐름

```text
ISBN
 → routers/generate.py
 → lookup_service.lookup_one
   → 국립중앙도서관 SearchApi
   → 정보나루 상세 / 키워드 / 이용분석 API
 → merge_biblio + merge_evidence
 → llm_input_builder.build_llm_input
 → marc_generator.build_prompt + rag_loader.load_rules
 → llm_client.generate
 → output_validator.validate_output
 → GenerateResult(fields, skipped_fields, warnings)
```

| 영역 | 주요 파일 | 분석 결과 |
|---|---|---|
| 외부 데이터 | `backend/clients/nl_client.py`, `data4library_client.py` | 서지값, 키워드, 책소개, 함께 대출된 도서 수집 |
| 병합·출처 | `backend/services/lookup_service.py:48-127` | 대체로 국중도 우선, 발행연도는 정보나루 우선. 선택된 일부 값에 `field_sources` 기록 |
| 추론 근거 | `backend/services/evidence_service.py` | keywords/description/co_loan_books/title/author/분류값/번역 정황 구성 |
| 생성 계약 | `backend/schemas/llm.py:19-64`, `llm_output.py` | 기본 생성 태그, 추론 근거 매핑, 결과 스키마 |
| 생성 | `backend/services/marc_generator.py:121-175` | **정형 필드까지 포함해 LLM이 전체 MARC JSON 생성** |
| 후처리 | `backend/services/output_validator.py` | JSON/스키마 확인, 650 제거, 무의미 값 제거, 653 오염어 필터 등 |
| 별도 검증 | `backend/routers/validate.py` | 주로 020/245/260 존재·필수 식별기호·ISBN 형식 검사. 완전한 KORMARC 규칙 검증 아님 |
| 교환파일 | `backend/routers/export.py` | pymarc를 통한 mrc/mrk/json. 제어필드·자료유형별 리더 정책은 별도 검토 필요 |
| 평가 | `ai/evaluation/evaluate_koma.py`, `evaluate_bulk.py` | 각각 gold 비교, 참조 정답 없는 대량처리 지표·수동검수 표 |

**중요: `source="api"`는 현재 “LLM 없이 생성했다”는 증거가 아니다.**

`marc_generator.py:170-175`는 LLM 결과를 후처리해 반환한다. `output_validator.py:87-100`은 태그가 020/245/260/700/710이면 `generated_by="api"`, 그 외에는 `"llm"`으로 사후 표기한다. 같은 파일 136-138행은 해당 태그에서 근거 없는 `ai_inference`를 `source="api"`로 바꾸기도 한다. 따라서 출처 라벨과 실제 생성 경로를 분리해서 개선해야 한다.

### 2.2 수집 가능한 값과 현재 수집하지 않는 값

- 국중도 매핑: ISBN·부가기호·세트 ISBN·가격·제목·저자·출판사·예정발행일·판사항·총서·권차·쪽수·크기·KDC·DDC 등.
- 정보나루 매핑: 제목·저자·출판사·발행연도·ISBN·분류번호·책소개, 별도 API의 키워드와 함께 대출된 도서.
- `pub_place`, `kdc_edition`, `ddc_edition`은 병합 코드에서 항상 빈 문자열이다 (`lookup_service.py:92,105,108`). **현 파이프라인만으로 발행지·분류표 판차를 확정할 수 없다.**
- 저자 정보는 역할별 구조화 배열이 아니라 문자열이다. 700/710 분리, 공저자·역자·그림작가 구분에는 추가 해석이 필요하다.
- API에 필드 매핑 코드가 있다는 사실과 각 도서에서 그 값이 실제 제공됐다는 사실은 다르다. 저장 결과만으로 당시 원본 API값을 복원할 수 없다.
- `biblio.subject`는 전달되지만 `field_evidence_map`의 주제어 근거로 명시되지 않는다. 이를 통제 주제명 대조 결과로 간주해서는 안 된다.

### 2.3 프런트·계약 조사 결과: 품질 개선의 연동 위험

다음은 정적 코드 분석이며 브라우저 재현은 이번 단계에서 하지 않았다.

- `frontend/src/api/marc.js`는 생성 API를 호출한다. 검증·백엔드 내보내기 API는 UI 경로에 연결되어 있지 않다.
- `MARCInput.jsx`는 생성 결과 배열을 콜백에 전달하지만 `HomeDashboard.jsx`는 단일 객체를 기대한다. **[INFERENCE] 검수 화면으로 결과 전달이 깨질 수 있다.**
- `MARCInput.jsx`의 API 오류 처리에 목업 결과 대체가 있다. 목업을 실제 생성 결과로 평가하면 안 된다.
- `MARCInspection.jsx`는 식별기호 단위 flat row로 편집하고, 저장 시 중첩 `fields/subfields` 형태를 보존하지 않는다. 부모에서 `onSave`도 전달하지 않는다.
- 프런트 TXT/JSON 다운로드는 백엔드 export와 별도 구현이다. 화면 출력과 ISO2709 내보내기의 품질을 같은 것으로 간주하지 않는다.
- `docs/api-spec.md`, `docs/marc-json-schema.md`, `shared/`는 초기 계약이 남아 있다. 현재 Pydantic 계약과 다르며, `backend/docs/KOMA_스키마_v2.1.md`에도 아직 구현되지 않은 prefix/display 등의 설계가 있다.

이 문제들은 별도 프런트 연동 작업으로 추적한다. **API/저장 JSON을 이용한 필드·RAG 개선 분석을 프런트 전체 개편이 끝날 때까지 미룰 필요는 없다.** 다만 최종 화면 시연·사서 검수까지 완료했다고 주장하려면 실제 화면 경로를 연결하고 확인해야 한다.

## 3. 현재 필드 목록과 세 가지 분류

### 3.1 집계 기준

`backend/schemas/llm.py:19-25` 기준:

- 기본 생성 시도 **17개**: 020, 041, 056, 082, 245, 246, 250, 260, 300, 490, 500, 546, 653, 700, 710, 830, 950.
- 별도 기본 제외 **1개**: 650. 규칙 파일은 있으나 기본 프롬프트 생성 태그에서 제외되고 후처리에서도 제거된다.
- 코드의 `required_fields`는 020/245/260/700/710이다. 이 명칭은 KORMARC 공식 필수성이나 실제 출력 보장과 같지 않다. 개인저자·단체저자를 무조건 둘 다 만들면 안 된다.
- 아래 생성 권수는 **중간발표 저장 JSON 33권에서 해당 태그가 있는 도서 수**다. 반복 필드 발생 횟수와 다르다. 예: 700은 23권에서 32회 발생한다.

분류는 **향후 책임을 어디에 둘 것인지(권장 분류)**를 나타낸다.

- **A — API 직접**: API의 명시된 서지값을 의미 추론 없이 채택. 형식 검사와 MARC 조립은 여전히 코드 책임.
- **B — 규칙 변환**: API 문자열을 식별기호로 분리하거나 날짜·단위·형식을 규칙으로 변환.
- **C — LLM 판단**: 비정형 문장의 역할·주제·주기 적합성 판단. 근거 없는 사실 생성은 금지.

**현재 실제 생성 경로는 태그와 무관하게 전부 LLM 1회 출력이다.** 세 범주 중 어느 것도 아직 코드로 분리돼 있지 않다.

| 구분 | 권장 분류 (이 문서의 A/B/C) | 현재 실제 경로 |
|---|---|---|
| 값을 만드는 주체 | 필드별로 코드 또는 LLM | 17개 태그 모두 LLM 1회 출력 |
| 코드의 역할 | 정형값 직접 생성, 규칙 변환, 형식 강제 | 생성 후 삭제·보정 중심 (`output_validator.py`) |
| `source="api"` 의미 | API 원본값을 코드가 채택함 | `020/245/260/700/710` 태그에 대한 **사후 라벨**. `output_validator.py:87-100`이 태그 집합만 보고 부여 |
| 근거 없는 추론 처리 | 생성 보류 | 위 태그면 `source="api"`로 승격되어 남는다 (`:136-138`) |

실측으로도 확인된다. 중간발표 저장 JSON 33권에는 `generated_by` 키 자체가 없고(`generated_by_labels=unset`), `source`는 태그 집합대로만 갈린다. 따라서 **지금의 `source="api"` 표기를 “API 직접 생성 완료”로 읽으면 안 된다.**

이 간극(정형·규칙 필드의 결정론 생성 레이어 부재)이 이번 개선의 최우선 항목이다. 아래 표의 권장 분류는 4단계에서 코드로 옮길 대상이다.

필드 하나에 여러 성격이 섞일 수 있으므로 권장 분류를 하나만 부여하고 예외를 명시한다. 특히 **API 값 부재를 자동으로 LLM 추론 허용으로 바꾸지 않는다.**

### 3.2 필드별 분류표

`권장` 열은 개선 목표, `현재` 열은 중간발표 시점 실제 동작이다.

| 태그 | 필드 | 권장 분류 | 현재 경로 / 현재 source 라벨 | 개선 원칙 | 생성 권수 / 33 | RAG 파일 |
|---|---|---|---|---|---:|---|
| 020 | 국제표준도서번호 | A | LLM / `api` 33 | isbn_ea·부가기호·가격·세트 ISBN 채택. ISBN 정규화·세트 구분·통화 표현은 규칙으로 처리 | 33 | 없음 |
| 041 | 언어부호 | C | LLM / `ai_inference` 5 | 번역 정황·책소개·저자 문자열 해석. 언어가 명시되면 부호 변환은 B. 저자 국적 등으로 원어 추측 금지 | 5 | 있음 |
| 056 | 한국십진분류기호 | A | LLM / `api` 13, `ai_inference` 8 | kdc를 우선 전사. 1자리 대분류·상세번호 구분, 판차 미수집 표시. 값이 없을 때 후보 추론은 별도 C 정책이며 확정값 아님 | 21 | 있음 |
| 082 | 듀이십진분류기호 | A | LLM / `ai_inference` 1 | ddc가 있어도 판·출처 근거 확인 필요. 현행 판차 미수집 상태에서는 기본 보류. KDC에서 임의 변환 금지 | 1 | 있음 |
| 245 | 표제와 책임표시사항 | B | LLM / `api` 33 | title·author·volume → `$a/$b/$d/$e/$n`. KORMARC 책임표시는 `$d/$e`. 모호한 역할 분해만 C, 원문 보존 | 33 | 없음 |
| 246 | 여러 형태의 표제 | C | LLM / `ai_inference` 1 | 현 매핑에 독립적인 원표제·변형표제 값이 없음. 명시 근거에서만 후보 추출, 가능성만으로 생성 금지 | 1 | 없음 |
| 250 | 판사항 | A | 생성 0권 | edition_stmt에 명시된 판사항 전사. 판사항이 없으면 보류, 초판 가정 금지 | 0 | 없음 |
| 260 | 발행·배포·간사사항 | B | LLM / `api` 32 | publisher·publish_year를 `$b/$c`로 조립. 발행예정일과 실제 발행일 구분. `$a`는 현재 근거 없음 | 32 | 없음 |
| 300 | 형태사항 | B | LLM / `api` 11 | page·book_size의 수량·단위·치수 변환. 삽화·면수·크기를 추측하지 않음 | 11 | 없음 |
| 490 | 총서사항 | B | LLM / `api` 3 | series_title·series_no 기술, 추적 정책에 맞는 지시기호 적용. 830과 자동 동일시 금지 | 3 | 없음 |
| 500 | 일반주기 | C | LLM / `ai_inference` 22 | 일반주기로 기술할 객관적 사실만 추출. 소개·홍보·줄거리 요약을 넣지 않음 | 22 | 있음 |
| 546 | 언어주기 | C | LLM / `ai_inference` 19 | 명시적 언어·번역 정보만 문장화. 언어가 구조화되면 B로 생성 가능. 한국어 표제만으로 생성 금지 | 19 | 있음 |
| 650 | 통제 일반주제명 | C | 기본 제외, 후처리에서 제거 | 주제 판단 외에 전거·표목표 대조 필수. LLM 단독 확정 불가 | 0 | 있음, 기본 미주입 |
| 653 | 비통제 색인어 | C | LLM / `ai_inference` 31 | 키워드·책소개에서 주제 선별. 다른 책 제목 유입 금지. 표제에 포함된 정상 주제어까지 제거하지 않도록 검토 | 31 | 있음 |
| 700 | 개인명 부출표목 | C | LLM / `api` 27, `ai_inference` 5 | author 문자열에서 인물·역할 구분. 명확한 구분자·역할어 처리는 B, 구조화 API 확보 시 A/B로 이동 가능 | 23 | 없음 |
| 710 | 단체명 부출표목 | C | 생성 0권 | 개인·단체 판단과 명칭 해석. 단체 근거가 있을 때만 생성, 모든 책에 필수 아님 | 0 | 없음 |
| 830 | 총서부출표목 | C | LLM / `api` 3 | 490의 기술형 총서명과 전거형 접근점은 다름. 전거·기관 추적 정책 확인 전 보류, 확정 매핑의 조립은 B | 3 | 없음 |
| 950 | 로컬 가격사항 | B | LLM / `api` 2 | price와 기관 로컬 규칙으로 변환. 종이책 여부·청구기호를 가격 필드로 혼동하지 않음 | 2 | 없음 |

`현재 source 라벨`의 `api`는 값의 출처가 아니라 태그 기반 사후 표기다. 700은 23권에서 32회 생성됐고, 같은 태그 안에서도 `api`와 `ai_inference`가 섞인다.

제어필드(001/005/007/008)나 초기 샘플의 100은 위 17개 생성 목록에 포함되지 않는다. 리더·제어필드는 LLM 추론 필드가 아니라 교환 레코드 생성 정책으로 따로 다룬다.

## 4. 기존 RAG의 상태와 보강 방법

### 4.1 실제 로딩 동작

- Markdown 원문: `ai/rag/docs/kormarc/fields/`.
- 런타임 입력: `ai/rag/chunks/kormarc-<tag>.jsonl`.
- `backend/services/rag_loader.py:28-85`는 태그별 파일을 통째로 읽어 프롬프트에 넣는다. 벡터 검색·유사도 순위 검색은 아니다.
- 규칙 파일 7개, 총 44개 청크: 041/056/082/500/546/650 각 6개, 653은 8개.
- 기본 생성 경로에서는 650이 제외되어 **17개 태그 중 6개만 태그별 RAG 규칙이 주입**된다.
- Markdown만 수정하면 런타임에 반영되지 않는다. Markdown과 JSONL을 함께 갱신해야 한다.
- 누락 파일·깨진 JSONL 줄을 로더가 건너뛰므로, 규칙 문서를 작성했다는 사실만으로 실제 적용을 보장하지 않는다.

### 4.2 먼저 확정할 규칙

1. **245 KORMARC/MARC21 혼용 제거**: 공식 KORMARC에서 책임표시는 $d/$e, $h는 자료유형이다. 제2지시기호도 MARC21의 비정렬 문자수 규칙을 가져오지 않는다.
2. **API 사실 보존**: API에서 채택한 값을 LLM이 다시 쓰거나 삭제하지 못하게 한다. 실제 채택 API·변환 규칙·추론 근거를 구분한다.
3. **결측 처리**: 발행지·분류표 판·쪽수·크기·원어가 없으면 빈 사실을 만들어 채우지 않는다. 유효한 부분 필드 보존과 필드 전체 보류를 구분한다.
4. **041/546/500 일관성**: 번역 감지와 원어 확인을 분리. “한국어 제목”은 원어·번역 여부의 근거가 아니다. 500에 막연한 책소개 문장을 넣지 않는다.
5. **056/082**: API 전사와 추론 후보를 분리. 056의 상위 분류를 상세 분류 일치로 평가하지 않는다. 082의 판·출처를 지어내지 않는다.
6. **653/650**: 비통제어를 650으로 승격하지 않는다. 주제어의 의미 적합성과 문자열 일치를 별도로 본다.
7. **490/830**: 총서 기술과 총서 접근점의 역할·연동을 정리한다. 근거 없는 830 복제 생성은 보류한다.
8. **가격/구두점/표시**: 020·950 역할과 기관 정책을 정리하고, 내부 값·화면 표시·실제 MARC 교환 시 구두점 책임을 구분한다. 서명 자체의 문장부호까지 일괄 삭제하지 않도록 한다.

규칙 문서마다 공식 출처·확인일, 생성 조건, 허용 지시기호/식별기호, 반복성, 근거 매핑, 금지 조건, 정상/오류 예시, 검증 항목을 기록한다. **공식 표준과 서비스의 보수적 생성 정책을 별도 항목으로 작성**한다.

이번 조사에서 직접 확인한 공식 자료:

- [KORMARC 통합서지용 목차](https://librarian.nl.go.kr/kormarc/KSX6006-0/index.html)
- [245 표제와 책임표시사항](https://librarian.nl.go.kr/kormarc/KSX6006-0/sub/20X_24X_245.html)
- [056 한국십진분류기호](https://librarian.nl.go.kr/kormarc/KSX6006-0/sub/01X_09X_056.html)

나머지 필드의 저장된 요약을 모두 공식 검증 완료로 간주하지 않는다. 규칙 반영 단계에서 각 공식 원문을 대조한다. 653 공식 URL은 이번 조회에서 서버 오류가 나 원문 확인을 완료하지 못했다.

### 4.3 추가 필드의 우선순위

“새 필드 추가” 전에 **이미 생성 중인데 규칙이 없는 필드**를 우선 보강한다.

- 1차: 020/245/260/300/700/710 규칙 및 코드 검증.
- 2차: 기존 조건부 246/250/490/830/950의 근거·보류·연동 규칙.
- 신규 후보: **504 서지 등 주기**. gold 33권 중 4권에 있다. 참고문헌/색인 관련 명시 근거를 수집할 수 있는지 확인한 뒤 채택한다. RAG 파일만 추가해서는 기본 생성 태그에 편입되지 않는다.
- 별도 교환 범위: 008 등 제어필드. 현재 출력 모델이 일반 데이터필드 중심이므로 별도 계약·직렬화 작업 필요.
- 100/264/502 등은 배경 PDF에 언급되지만 일반서 범위·KORMARC 적용·입력 근거를 확인하기 전 자동 생성 대상으로 늘리지 않는다.
- gold에 있는 440 등 기관 레코드의 모든 태그를 무조건 그대로 신규 채택하지 않는다. 기관 관행·현행 표준·서비스 범위를 함께 확인한다.

## 5. 저장된 33권을 직접 확인한 결과

### 5.1 실제 실행한 검증

기존 `evaluate_koma.py`를 `--skip-generate`로 실행했다. 입력은 `mid_result/eval_results`와 `docs/`의 gold `.mrc`, 출력은 임시 디렉터리로 분리했다.

```sh
python3 ai/evaluation/evaluate_koma.py --skip-generate \
  --results-dir mid_result/eval_results \
  --summary-csv <임시경로>/eval_summary.csv \
  --summary-old-csv <임시경로>/eval_summary_old.csv \
  --books-csv <임시경로>/eval_books.csv \
  --details-json <임시경로>/eval_details.json \
  --isbns-txt <임시경로>/eval_isbns.txt
```

- gold 33권 파싱, 저장 JSON **33/33권 비교**, 종료 코드 0.
- RAG 로더도 실제 호출해 7개 파일의 로딩과 나머지 태그의 규칙 부재를 확인했다.
- **새 API 조회나 LLM 생성은 실행하지 않았다.** 현재 코드 개선 효과를 측정한 것이 아니라 중간발표 기준선을 재현한 것이다.

**재평가 산출물과 저장본의 바이트 차이를 먼저 규명했다.** 지표값 차이는 없다.

| 파일 | 바이트 일치 | 원인 | 지표 동일 |
|---|---|---|---|
| `eval_summary.csv` | 불일치 | 재평가는 줄바꿈 `CRLF`(csv 모듈 기본), 저장본은 `LF` | 행·값 전부 동일 |
| `eval_books.csv` | 불일치 | 같은 줄바꿈 차이 | 행·값 전부 동일 |
| `eval_isbns.txt` | 일치 | — | 동일 |
| `eval_details.json` | 불일치 | `gold_mrc`에 **작성자 절대경로**(`/Users/minseo/Desktop/koma/...`)가 박혀 있음 | `summary`·`books` 페이로드 33권 전부 동일 |

즉 차이는 **줄바꿈 문자와 실행 환경 절대경로뿐이며, 평가기 로직이나 수치 변경이 아니다.** 이 문서가 인용하는 수치의 기준선은 `mid_result/eval_summary.csv`(커밋 `27d89c3`)이고, 위 명령으로 값이 재현된다.

`pymarc` 의존성도 확인했다. `evaluate_koma.parse_gold_records`는 `pymarc`가 있으면 그 파서를, 없으면 자체 파서를 쓴다. 두 경로로 각각 실행한 결과 summary·books·details·ISBN 순서가 모두 동일했다. 따라서 이 gold 파일에서는 `pymarc` 설치 여부가 기준선에 영향을 주지 않는다.

| 필드 | gold 보유 권수 | gold 보유 권수 중 생성 | 기존 평가 정확도 | 해석 |
|---|---:|---:|---:|---|
| 020 | 33 | 33 | 100% | ISBN 비교값 기준. 정형성은 50% |
| 245 | 33 | 33 | 93.94% | 제목 $a만 평가하므로 책임표시 오류를 놓침 |
| 260 | 33 | 32 | 78.12% | 발행지 $a는 평가 대상 밖 |
| 300 | 33 | 11 | 18.18% | 미생성 22권. API 결측/전사/변환 실패를 분리해야 함 |
| 041 | 3 | 1 | 100% | 생성된 1권 기준. gold가 없는 4권의 추가 생성은 정확도 계산에서 제외 |
| 056 | 33 | 21 | 23.81% | 상세번호 불일치·상위 분류 후보를 구분해야 함 |
| 653 | 33 | 31 | 74.19% | 부분일치를 정확으로 포함. P=0.3139, R=0.2371, F1=0.2624 |

250·082는 gold가 0권이라 현재 자료만으로 정확도를 평가할 수 없다. 082는 저장 결과에서 1권 생성됐지만 평가에서 제외된다.

### 5.2 구체적인 오류·검수 사례

대조 근거: `mid_result/eval_results/<ISBN>.json` 및 gold `.mrc`. 여기서 gold와 다르다는 사실만으로 API나 실물 중 어느 쪽이 틀렸는지까지 확정하지 않는다.

| ISBN / 도서 | 실제 저장 결과 | 대조 결과 / 문제 | 우선 조치 |
|---|---|---|---|
| 9791194630678 / 모두의 노션 AI | 245$c에 `임대균 오가연 지음`; 546 `한국어로 된 자료`; 300 없음 | 공식 245 책임표시는 $d/$e. gold에는 300 `190 p.`, `26 cm`. 단순 한국어 추정 546은 기존 서비스 규칙과도 충돌 | 245 구조 규칙, 546 근거 게이트, 300 원본 근거 확인 |
| 9791194090397 / gold 표제: 열두 살 어린 임금 단종이야기 | 제목 `단종 애사`; 245$h `장선경`; 300 `160 p`, `128 x 188`; 056 `813` | gold 제목·형태사항(`120 p.`, `23 cm`)·분류(`911`)와 다름. $h에 저자명은 잘못된 용도 | 잘못된 필드 배치 차단. 도서 동일성/API 선택값/실물 대조 후 값 오류 원인 확정 |
| 9788960909830 / 카프카의 문장들 | 300 `399 p`, `120*192`; 056 `8`; 041 kor/ger 있으나 546 보류 | gold 400p/20cm, KDC854. 041 생성과 546의 “조건 미충족” 사유가 정합적인지 검수 필요 | 단위 정규화, 1자리 분류의 한계 표시, 언어 관련 교차검증 |
| 9791194100133 / 우리는 죽으면 어디로 가나요? | 제목 `우리는 죽으면 어디로 갈나요`; 041 없음; 546 `한국어로 된 그림책` | gold 표제와 철자가 다르고, gold에는 041 kor/spa·스페인어 번역 주기 | 표제 채택값 추적. 번역·원어 근거 없으면 추측 대신 보류 |
| 9788961555951 / 7학년을 로딩 중 | 출판사 `파랑새`; 300 `174 p`; 041 번역물 표시; 056 `813.6` | gold 출판사 `열림원어린이`, 176p, KDC813.8, 041 없음 | API 충돌·판본 확인, 번역 판단 근거 검수. gold의 041 부재만으로 오생성 확정하지 않음 |
| 9791192638843 / 오늘내일하는 사이 | 082$a `808.4`, 지시기호 공백, 판차 없음 | 자체 RAG의 판 근거 미확정 시 보류 정책과 충돌. gold 082 부재라 현 평가에서는 제외 | 판·출처 없는 082 자동 생성 차단 |
| 9791199136250 / PD가 묻고 운명이 답하다 | 245와 동일한 246, reasoning에 `변형 표제 가능성` | 확인된 대체표제가 아니라 가능성으로 생성 | 246의 명시 근거 조건, 동일 표제 중복 검사 |
| 9788972979968 / 해당 ISBN 저장 결과 | 950$a `종이책` | 가격 로컬 필드에 자료형태가 들어감 | 950 역할·식별기호 규칙 및 기관 정책 확인 |

전체 33권 중 **28권에 245$c**, **1권에 저자명을 담은 245$h**가 있었다. 이 문제는 제목 정확도 93.94%라는 수치에 드러나지 않는다.

원인 분류는 다음 네 가지로 나누어 기록한다.

1. **표준/변환 오류**: 잘못된 식별기호, 단위, 지시기호, 반복 구조.
2. **입력 데이터 문제**: API 오기·상충·판본 불일치·실제 결측. 저장 결과만으로 원인 확정 불가.
3. **추론/정책 오류**: 근거 없는 언어·주기·분류, 전거 없는 접근점.
4. **평가 문제**: 서브필드 미평가, gold 부재 제외, 부분일치 과대 해석.

### 5.3 평가기를 먼저 보완해야 하는 이유

- `evaluate_koma.py:63-71`: 245는 $a만, 260은 $b/$c만, 300은 $a/$c만 비교한다. 책임표시·발행지·삽화는 누락된다.
- `evaluate_koma.py:698-709`: 041 내용 비교는 $a 중심이다. 원어 $h의 정확도를 별도 측정해야 한다.
- `regularity_score`는 gold와 지시기호/식별기호 목록이 같은지 보는 점수다. **공식 KORMARC 적합성 점수 자체가 아니다.**
- `metric_row`는 gold에 없는 필드를 제외한다. 근거 없는 추가 041/082/500/546이 있어도 현재 점수만으로는 잡히지 않는다.
- 653은 gold의 650+653을 합쳐 비교하고, 부분일치도 accurate로 센다. 따라서 “정확도 74.19%”와 “주제어가 74.19% 정확하다”는 다른 말이다.
- gold 주제어에도 서명·저자 관련 문자열이 있고 서비스는 이를 제외하려 한다. 서비스 정책과 gold 문자열 일치를 혼동하지 말고 사서의 의미 적합성 판정을 함께 기록한다.
- `evaluate_bulk.py:207-229`는 필드 발생 횟수를 성공 도서 수로 나눈다. 반복 700 등이 여러 개이면 presence rate가 100%를 넘을 수 있다. 도서별 존재율과 필드 발생 횟수를 분리해야 한다.

기존 지표를 보존하면서 **개선된 평가기로 기존 결과와 신규 결과를 모두 다시 평가**한다. 평가 기준만 바뀐 상승을 생성 품질 향상으로 보고하지 않는다.

### 5.4 평가 대상 밖 필드의 과잉·과소 생성 (기준선 스냅샷)

`evaluate_koma.py`가 채점하지 않는 태그까지 gold와 대조하면 아래 격차가 드러난다. 수치는 `ai/evaluation/baseline/mid-presentation/field_inventory.csv`에서 나온다.

| 태그 | 생성 권수 | gold 보유 권수 | 성격 |
|---|---:|---:|---|
| 546 언어주기 | 19 | 3 | **과잉 생성**. `9791194630678`의 `$a 한국어로 된 자료`처럼 일반 사실만으로 조건부 필드를 만든 사례 |
| 500 일반주기 | 22 | 6 | **과잉 생성**. 책소개 요약을 주기로 옮긴 사례가 섞임 |
| 700 개인명 | 23 | 33 | **과소 생성**. gold는 116회, 생성은 32회 |
| 950 로컬정보 | 2 | 33 | **과소 생성**. 기관 로컬 정책 필드 |
| 490 총서사항 | 3 | 0 | gold는 총서를 `440`(17권)으로 기술. 필드 선택 정책 자체가 다름 |
| 830 총서부출 | 3 | 0 | 490과 함께 생성됐으나 gold에 대응 필드 없음 |
| 246 여러 형태의 표제 | 1 | 4 | 생성 1건은 245와 동일 표제 |
| 041 언어부호 | 5 | 3 | gold에 없는 4권에서 추가 생성 |

`546`은 자체 RAG 규칙이 “단순히 한국어 책이라는 추정만으로 546을 생성하지 않는다”라고 명시했는데도 19권에서 생성됐다. **규칙 문장만으로는 금지가 집행되지 않는다**는 직접 증거다.

### 5.5 기준선 스냅샷 산출물

`ai/evaluation/baseline_snapshot.py`를 추가해 결과 디렉터리의 필드 현황을 고정했다. 이 스크립트는 결과 JSON과 gold만 읽고 원본을 수정하지 않으며, API·LLM도 호출하지 않는다.

```sh
python3 ai/evaluation/baseline_snapshot.py --label mid-presentation
```

출력 위치: `ai/evaluation/baseline/mid-presentation/`

| 파일 | 내용 |
|---|---|
| `manifest.json` | 실행 조건, gold·결과 33개 파일 SHA-256, 필드 정책 기본값, RAG 청크 현황, skip 사유 분포, 확인 불가 항목 |
| `field_inventory.csv` | 태그 31개의 정책 구분, 프롬프트 생성 대상 여부, RAG 규칙 도달 여부, 현재 생성 경로, source 라벨 분포, 생성·skip·gold 건수, 평가 대상 여부 |
| `book_field_matrix.csv` | 도서 x 태그 582행. 생성 횟수, source·generated_by 라벨, 검수 필요 수, skip 사유 |

이후 개선 결과는 `--label`을 바꿔 같은 형식으로 남기고, 새 생성 결과는 `mid_result/`가 아니라 별도 디렉터리(예: `ai/evaluation/runs/<label>/results`)에 저장한다.

기록에서 **확인 불가로 명시한 항목**(추정하지 않음):

- 중간발표 결과 생성 당시의 LLM 모델명과 호출 파라미터. 결과 JSON에 없다.
- 당시 API 조회 시점과 원본 응답. 저장돼 있지 않아 값 결측 여부를 사후 확인할 수 없다.
- 당시 `D4L_SKIP_USAGE` 설정값.

참고로 저장된 33권에는 `generated_by` 키가 아예 없다(`generated_by_labels=unset`). 현재 코드가 부여하는 `generated_by`는 중간발표 이후 추가된 것이므로, 전후 비교 시 이 필드의 유무를 품질 변화로 읽지 않는다.

## 6. 실제 작업 순서와 완료 조건

### 1단계 — 기준선·작업 범위 확정 (완료)

- 이 문서의 17개 생성 태그 + 기본 제외 650을 현재 목록으로 확정했다. 실제 저장 결과에도 이 목록 밖의 생성은 없었다.
- `mid_result/`와 gold `.mrc`는 수정하지 않았다. 39개 파일의 SHA-256을 작업 전후로 비교해 변경 없음을 확인했다.
- 기준선 재평가 결과가 저장본과 같은 값임을 확인하고, 바이트 차이의 원인(줄바꿈, 절대경로)을 규명했다(5.1).
- 필드 현황을 `ai/evaluation/baseline/mid-presentation/`에 고정했다(5.5).
- 모델명·API 조회 시점·`D4L_SKIP_USAGE`는 기록이 없어 `manifest.json`에 **확인 불가**로 남겼다. 추정값을 채우지 않았다.
- 다음 재생성부터는 lookup 근거·채택 API값을 함께 남긴다. API 키나 인증정보는 저장하지 않는다.
- **완료 조건 충족**: 기존 33권 재평가 재현, 입력·출력·정책 버전 추적 가능.

### 2단계 — 필드 분류·KORMARC 규칙·오류표 확정

- 위 표를 식별기호 단위로 세분화한다: API 키, 채택 우선순위, 변환 규칙, 결측/충돌 정책, LLM 사용 여부.
- 공식 규칙과 기관 로컬 정책을 분리한다. 우선 245 책임표시, 300 단위, 056/082 판, 041/546 조건을 확정한다.
- 위 사례를 `ISBN / 태그·식별기호 / 입력 근거 / 실제 출력 / 기대값 또는 보류 / 오류 종류 / 규칙 출처 / 확인 상태`로 관리한다.
- 실물이나 API 원본이 없는 값은 “확정 오류”와 “검수 필요”를 구분한다.
- **완료 조건**: 주요 필드마다 생성/보류 기준과 최소 정상·오류 사례가 있다.

### 3단계 — 평가·검증 기준 보완

- 책임표시·원어·추가 생성 오류·반복성·서브필드 유효성을 평가에 추가한다.
- 700/710/500/546 및 조건부 필드도 적절한 지표 또는 수동검수로 포함한다.
- 필드 존재율, 값 정확도, 공식 형식 위반, 근거 없는 생성, 올바른 보류를 분리한다.
- 250/082/710 등 기준 자료가 부족한 태그는 별도 명시 근거 도서를 확보해야 한다.
- **완료 조건**: 245$c 오류와 판 없는 082 사례가 평가/검증에서 드러나며, 기존 기준선의 재평가값을 보존한다.

### 4단계 — API 직접/규칙 생성과 LLM 판단 분리

- 020, 명시된 판사항·분류값 등은 API 근거로 코드에서 생성한다.
- 245/260/300/490/950 등은 확정된 변환 규칙으로 처리한다. 모호한 부분만 보류/검수한다.
- 확정 필드를 LLM이 다시 쓰지 않도록 생성 대상과 최종 병합 우선순위를 바꾼다.
- LLM은 653·주기·모호한 역할 분해처럼 실제 판단이 필요한 부분만 담당한다.
- `source`와 `generated_by`를 태그 기반 추정이 아니라 실제 경로·근거로 기록한다. 스키마·검수 표시·평가도 같은 의미를 사용한다.
- **완료 조건**: 동일 입력에서 정형 필드 값이 안정적으로 같고, LLM 변경으로 API 채택값이 바뀌지 않으며 근거 없는 api 라벨이 생기지 않는다.

### 5단계 — RAG 보강·조건부 필드 정비

- 4장의 우선순위대로 Markdown + JSONL + 생성 옵션/근거 매핑 + 후처리 규칙을 함께 갱신한다.
- 판단 규칙은 RAG에, 강제 가능한 형식·근거 조건은 코드 검증에 둔다. RAG 문장만으로 금지를 보장하지 않는다.
- 041/546/500의 일관성, 056/082의 판·출처, 653의 과도한 제거, 490/830 연동을 오류 사례로 검증한다.
- 신규 504는 근거 확보와 규칙·스키마·평가가 준비됐을 때만 편입한다. 650은 전거 연동 전 기본 보류를 유지한다.
- 벡터DB 교체는 이번 문제의 선행조건이 아니다. 현재 태그 기반 로딩 구조를 유지하며 내용과 집행을 개선한다.
- **완료 조건**: 변경한 규칙이 실제 프롬프트에 들어가고, 금지된 사례가 코드에서도 차단/보류된다.

### 6단계 — 같은 도서로 재생성·직접 검수·보고

- 기존 33권을 새 출력 경로로 재생성한다. 기존 JSON을 덮어쓰지 않는다.
- 위 상세 사례를 먼저 확인하고, 일반서·번역서·그림책·총서·복수저자·결측 사례를 포함해 최소 5~10권을 직접 검수한다.
- 동일 평가기로 before/after를 비교한다. 규칙 변환 개선과 LLM 주제 판단 개선을 나눠 보고한다.
- 관련 기존 테스트를 실행하고, 잘못된 책임표시·불명확한 단위·근거 없는 언어·반복필드 등의 소비자 영향 오류에 회귀 검증을 둔다.
- 최종 화면까지 납품 범위에 포함한다면 2.3의 연결 문제를 해결한 뒤 입력→검수 수정→재검증→실제 mrc 왕복을 확인한다. JSON만 좋아졌다고 화면/반입도 완료로 보고하지 않는다.
- **완료 조건**: 필드 목록·3분류표, 공식 규칙 문서/청크, 전후 오류 사례, 동일 기준의 평가표, 남은 결측/검수 항목을 제출한다.

권장 커밋 단위는 분석 문서 → 평가/검증 → 정형·규칙 생성 → RAG/조건부 규칙 → 결과 보고다. 각 구현 커밋에서 영향받는 스키마·호출부·문서·샘플을 함께 맞춘다.

## 7. 진행 로그

사용자가 지정한 9단계 순서로 진행한다. 각 단계는 실행 결과로 확인한 것만 완료로 적는다.

### 1단계 — 기존 33권 결과 백업 (완료)

- `mid_result/`와 gold `.mrc`는 커밋 `27d89c3` 이후 변경되지 않았고, 이번 작업에서도 수정하지 않았다.
- `ai/evaluation/baseline_snapshot.py`에 `--verify`를 추가했다. `manifest.json`의 SHA-256과 현재 파일을 대조한다.
- 확인: 정상 상태에서 `기준선 무결성 확인`, 파일을 1바이트 바꾸면 종료 코드 1과 변경 파일명을 출력한다(되돌린 뒤 재확인 완료).

```sh
python3 ai/evaluation/baseline_snapshot.py --label mid-presentation --verify
```

### 2단계 — 245 규칙 정리 (완료)

- 공식 [245 표제와 책임표시사항](https://librarian.nl.go.kr/kormarc/KSX6006-0/sub/20X_24X_245.html)을 대조해 규칙 문서와 청크를 만들었다.
  - `ai/rag/docs/kormarc/fields/245-title-statement.md`
  - `ai/rag/chunks/kormarc-245.jsonl` (8청크, 로더 로딩 확인)
- 확정한 핵심 규칙
  - 책임표시는 `$d`(첫 번째) / `$e`(두 번째 이하). **245에 `$c`는 없다.**
  - `$h`는 자료유형표시. 저자명·부제를 넣지 않는다.
  - 제1지시기호는 `0`. 이 서비스는 1XX 기본표목을 만들지 않는다. gold 33권도 모두 `00`이다.
  - 부제는 표제에 구분자가 있을 때만 분리한다. 없으면 만들지 않는다.
  - 값에 ISBD 구두점을 넣지 않는다.

### 3단계 — 245 평가 코드 수정 (완료)

`ai/evaluation/evaluate_koma.py`를 고쳤다.

| 변경 | 내용 |
|---|---|
| 비교 범위 | 245를 `$a`만 → **본표제·부제·책임표시 3성분**으로 비교 |
| 책임표시 비교 | `$d`/`$e`에서 역할어·각괄호를 걷어내고 인명 집합 F1로 채점 |
| 구조 위반 | 공식 문서로 확인한 020/245/260/300/056의 허용 식별기호 외 코드, 245 반복 생성, `$a` 누락, `$h` 오용을 위반으로 기록 |
| 정확 판정 | 값이 맞아도 구조 위반이 있으면 `accurate=False` |
| 출력 | summary에 `violation_books`, books CSV에 `violations`·`245_score`·`245_components` 추가 |

저장된 33권에 적용한 결과, **29권에서 구조 위반이 드러났다**(`245$c` 28권, `245$h` 1권). 기존 평가는 이를 전혀 잡지 못했다.

| 245 지표 | 기존 기준 | 새 기준(같은 결과물) |
|---|---:|---:|
| 정확도 | 93.94% | 6.06% |
| exact | 31 | 2 |
| 구조 위반 도서 | 측정 안 함 | 29 |

**이 하락은 생성 품질 저하가 아니라 평가 기준 교정이다.** 이후 비교는 모두 새 기준으로 한다.

회귀 테스트: `backend/tests/test_evaluate_koma_245.py` 8건.

### 4단계 — API → 245 직접 생성 (완료)

`backend/services/deterministic_fields.py`를 새로 만들었다. **규칙 레이어의 첫 필드가 245다.**

- `build_245(biblio)`가 `title`/`volume`/`author`를 KORMARC 식별기호에 배치한다. LLM을 호출하지 않는다.
- `marc_generator.select_generation_tags`가 245를 LLM 생성 대상에서 제외한다.
- `generate_marc`는 규칙 결과를 기준으로 합친다. LLM이 245를 만들어도 무시하고 경고를 남긴다.
- `output_validator._assign_generated_by`를 고쳤다. **LLM 출력은 태그와 무관하게 `generated_by="llm"`**이고, 규칙 레이어 필드만 `"rule"`이다. 태그만 보고 `"api"`를 붙이던 기존 라벨은 실제 생성 주체와 달라 검수자와 지표를 오도했다.

검증(규칙 레이어 미리보기): 저장된 33권의 표제·저자 문자열을 입력으로 넣어 245만 규칙 레이어 결과로 바꾼 뒤 같은 평가기로 측정했다.

| 245 지표(새 기준) | 중간발표 결과 | 규칙 레이어 미리보기 |
|---|---:|---:|
| 구조 위반 도서 | 29 | **0** |
| 정확도 | 6.06% | 39.39% |
| exact | 2 | 13 |
| mismatch | 2 | 0 |

남은 차이는 대부분 부제(`$b`)와 책임표시 누락이다. 저장 결과에는 부제와 저자 문자열이 이미 빠져 있어, 실제 API 값으로 재생성하는 8단계에서 다시 측정해야 한다.

**주의**: 이 미리보기의 입력은 저장된 245 값이다. 당시 API 원본이 저장돼 있지 않아 쓴 대체 입력이며, 8단계의 실제 재생성 결과와 같지 않다.

테스트: `backend/tests/test_deterministic_fields.py` 13건, `backend/tests/test_marc_generator.py` 병합 동작 2건. 백엔드 전체 126건 통과.

### 5단계 — 300, 056, 082, 041/546 (완료)

**300 / 056 / 082는 규칙 레이어로 옮겼다.** 값이 없으면 LLM에 넘기지 않고 skip 사유를 남긴다.

| 태그 | 입력 | 규칙 | 값이 없을 때 |
|---|---|---|---|
| 300 | `page`, `book_size` | 수량은 API 단위(장·책·권·면·매) 유지, 없으면 `p.`. 크기는 mm면 세로값을 cm로 올림, cm면 그대로. 단위 불명이면 생성 안 함 | `형태사항 근거 없음` |
| 056 | `kdc`, `kdc_edition` | `$a` 전사, 판차 있으면 `$2`. 지시기호 공백 | `KDC 근거 없음` (키워드 추론 금지) |
| 082 | `ddc`, `ddc_edition` | `$a` 전사. 판 유형·부여 출처 근거가 없어 지시기호 공백 | `DDC 근거 없음` (KDC 변환 금지) |

**041 / 546은 LLM이 담당하되 검증기가 근거 없는 생성을 제거한다.**

- 041: 본문언어 `$a` 하나뿐이고 번역 신호가 없으면 제거. 008/35-37에 이미 있는 정보라 추가 가치가 없다.
- 546: `한국어로 된 자료`처럼 단일 언어 추정만 담은 주기는 제거. 번역·원작·대역·병기·자막·요약 같은 언어 정보가 있으면 유지.

공식 원문 대조: [041](https://librarian.nl.go.kr/kormarc/KSX6006-0/sub/01X_09X_041.html), [082](https://librarian.nl.go.kr/kormarc/KSX6006-0/sub/01X_09X_082.html), [300](https://librarian.nl.go.kr/kormarc/KSX6006-0/sub/3XX_300.html), [546](https://librarian.nl.go.kr/kormarc/KSX6006-0/sub/5XX_546.html).

### 6단계 — LLM 담당 필드 정리 (완료)

순수 전사 필드를 모두 규칙 레이어로 옮겨 **생성 주체를 분리**했다.

| 구분 | 태그 | 생성 주체 |
|---|---|---|
| 규칙 레이어 | 020, 245, 250, 260, 300, 490, 056, 082 | `backend/services/deterministic_fields.py` |
| LLM | 700, 710, 653, 500, 546, 041, 246 | 프롬프트 + RAG 규칙 |
| 기본 제외 | 650, 830, 950 | 생성하지 않고 사유 기록 |

- 650: 표목표 대조 근거 없음(기존 정책 유지).
- 830: 총서부출표목은 전거·기관 정책이 필요하다. 490을 복사해 만들지 않는다.
- 950: 기관 로컬 필드다. 자관 규칙 없이 자동 생성하지 않는다. 중간발표 결과의 `950$a 종이책` 같은 오류를 원천 차단한다.
- 710을 `required_fields`에서 `conditional_fields`로 옮겼다. 일반 도서에 단체저자는 대부분 없다.

### 7단계 — RAG 수정 (완료)

| 작업 | 대상 |
|---|---|
| 신규 규칙 문서·청크 | 020, 250, 260, 300, 490 (규칙 레이어), 700, 710, 246 (LLM) |
| 정책 반영해 재작성 | 056, 082 (규칙 레이어로 전환), 041, 546 (근거 조건 강화) |
| 유지 | 500, 653, 650 |

각 문서에 `generation_path`(rule/llm)를 명시해 생성 주체를 규칙 자체에 기록했다.

확인: **LLM 생성 대상 7개 태그 전부 RAG 규칙이 주입된다.** 중간발표 시점에는 17개 중 6개뿐이었다.

```text
LLM 생성 대상: ['700', '653', '041', '246', '500', '546', '710']
규칙 주입:     ['041', '246', '500', '546', '653', '700', '710']
규칙 없는 LLM 태그: []
```

### 8단계 — 같은 33권 다시 생성 (미완료: API 키 없음)

**실행하지 못했다.** 재생성은 국중도·정보나루·LLM을 실제로 호출해야 하는데, 이 저장소에는 `.env`가 없고 `NL_API_KEY`/`D4L_API_KEY`/`OPENAI_API_KEY` 환경변수도 설정돼 있지 않다. 키 없이 만든 결과를 재생성 결과로 보고할 수는 없다.

키가 준비되면 아래 순서로 실행한다.

```sh
# 1) 키 설정 (backend/.env.example 참고, 커밋 금지)
cp backend/.env.example backend/.env && $EDITOR backend/.env

# 2) 서버 기동
uvicorn backend.main:app --port 8000

# 3) 기존 결과를 덮어쓰지 않도록 새 디렉터리로 재생성 + 평가
python3 ai/evaluation/evaluate_koma.py \
  --results-dir ai/evaluation/runs/after-rule-layer/results \
  --summary-csv ai/evaluation/runs/after-rule-layer/eval_summary.csv \
  --summary-old-csv ai/evaluation/runs/after-rule-layer/eval_summary_old.csv \
  --books-csv ai/evaluation/runs/after-rule-layer/eval_books.csv \
  --details-json ai/evaluation/runs/after-rule-layer/eval_details.json \
  --isbns-txt ai/evaluation/runs/after-rule-layer/eval_isbns.txt

# 4) 필드 현황 스냅샷 + 기준선 훼손 여부 확인
python3 ai/evaluation/baseline_snapshot.py --label after-rule-layer \
  --results-dir ai/evaluation/runs/after-rule-layer/results \
  --note "모델·조회시점·D4L_SKIP_USAGE 값을 여기에 기록"
python3 ai/evaluation/baseline_snapshot.py --label mid-presentation --verify
```

재생성 시 `D4L_SKIP_USAGE` 값을 반드시 기록한다. 이 플래그가 켜져 있으면 `co_loan_books` 근거가 빠져 653 결과가 달라진다.

대신 **전체 경로가 실제로 동작하는지는 확인했다.** 외부 호출만 대역으로 두고 라우터→규칙 레이어→검증기를 실제 코드로 통과시켰다(`backend/tests/test_generate_pipeline.py`).

```text
020    $a9791194630678$g13000$c₩13000        [api/rule]
245 00 $a모두의 노션 AI$b초보자도 바로 써먹는 노션 입문서$d임대균 오가연 지음   [api/rule]
260    $b생능북스$c2026                        [api/rule]
300    $a190 p.$c26 cm                       [api/rule]
490 00 $a모두의 시리즈$v12                     [api/rule]
056    $a005.58                              [api/rule]
653    $a인공지능$a업무관리                     [ai_inference/llm]
skipped: 546(단일 언어 추정), 250(판사항 미수집), 082(DDC 미수집)
```

LLM이 예전과 같은 `245$c`와 `546 한국어로 된 자료`를 출력해도 최종 결과에서 제거됐다. 이 도서의 gold 300은 `190 p. : 천연색삽화 ; 26 cm`로, 규칙이 만든 `190 p.`/`26 cm`와 일치한다(삽화는 근거가 없어 생성하지 않음).

### 9단계 — 기존 결과 vs 새 결과 비교 (오프라인 범위만 완료)

실제 재생성이 막혀 있어, **저장된 33권을 새 파이프라인에 통과시킨 오프라인 비교**를 했다. 규칙 레이어와 검증기는 실제 코드이고, 입력만 저장 결과에서 역산한 대체값이다.

평가는 양쪽 모두 새 평가 기준(3단계)으로 했다.

| 필드 | gold | 생성 권수 | 정확 건수 | 구조 위반 도서 |
|---|---:|---:|---:|---:|
| 020 | 33 | 33 → 33 | 33 → 33 | 0 → 0 |
| 245 | 33 | 33 → 33 | 2 → **13** | 29 → **0** |
| 260 | 33 | 32 → 32 | 25 → 25 | 0 → 0 |
| 300 | 33 | 11 → 11 | 2 → 0 | 0 → 0 |
| 056 | 33 | 21 → 21 | 5 → 5 | 0 → 0 |
| 653 | 33 | 31 → 31 | 23 → 23 | 0 → 0 |
| **전체** | | 완전성 0.7446 유지 | 정확도 **0.5719 → 0.5936** | **29 → 0** |

과잉 생성도 줄었다.

| 태그 | 생성 권수 | gold 보유 권수 |
|---|---|---:|
| 546 | 19 → 0 | 3 |
| 041 | 5 → 1 | 3 |
| 082 | 1 → 0 | 0 |
| 830 | 3 → 0 | 0 |
| 950 | 2 → 0 | 33(기관 로컬) |

**이 비교의 한계(반드시 감안할 것)**

- 입력이 실제 API 값이 아니라 저장된 생성 결과에서 역산한 값이다. 예를 들어 300은 저장값 `152 x 224`에 단위가 없어 크기가 빠졌고, 그래서 정확 2건이 0건이 됐다. 실제 API `book_size`는 `188*257mm`처럼 단위가 있어 위 스모크에서는 `26 cm`가 정상 생성됐다.
- 546이 0권이 된 것도 대체 입력에 번역 신호가 없기 때문이다. 실제 재생성에서는 번역서 3권에서 유지될 것으로 본다. **[INFERENCE]**
- 따라서 **값 정확도 개선 폭은 8단계 재생성 후에 확정해야 한다.** 지금 확정적으로 말할 수 있는 것은 구조 위반 29 → 0과 근거 없는 과잉 생성 감소다.

### 10단계 — 평가 기준 v2 수립과 재평가 (완료)

8단계 재생성은 2026-09-25 로컬에서 수행됐다(`eval_results/`). 이 결과를 `ai/evaluation/runs/after-rag/results`로 복사해 고정했고, 필드 현황은 `ai/evaluation/baseline/after-rag/`에 스냅샷으로 남겼다. 모델·`D4L_SKIP_USAGE`는 `backend/.env`에 지정이 없어 config 기본값으로 기록했다(셸 환경변수는 미확인).

**왜 v2가 필요한가.** 보강된 RAG의 핵심은 "근거가 없으면 만들지 않는다"이다. v1의 생성률·정확도로는 올바른 보류가 감점되고, 근거 없는 과잉 생성(출판사를 710에, 245와 같은 246)은 드러나지 않는다. v1은 기준선 재현을 위해 그대로 두고, `ai/evaluation/evaluate_v2.py`를 새로 만들었다.

| 축 | 무엇을 보나 | 분모 |
|---|---|---|
| A 값 정확도 | gold와 값 비교. 부분점수 평균과 exact 비율. 700 인명 F1 신설, 245 책임표시는 역할어를 토큰 단위로 제거, 653은 F1 | gold가 있는 (도서, 필드). 생성 안 했으면 0점 |
| B RAG 규칙 준수 | `ai/rag/chunks` generation/forbidden 규칙 18개를 코드 검사로 옮김 (245 구조, 발행지 추정, 300 단위, 830·950 금지, 546 무근거, 700=245 인원, 출판사 710, 중복 246, evidence 등) | 해당 필드가 있는 (도서, 규칙) |
| C 생성 판단 | 태그 15개의 생성/보류 결정. gold에 없고 생성도 안 했으면 정답(TN) | 전체 (도서, 태그) |
| KOMA-Q | (A + B + C) / 3 | |

v1 대비 채점 보정 두 가지: ① v1 책임표시 토크나이저가 `엮고 옮긴이`→`엮고`, `저자`→`자`를 인명으로 남기던 문제를 토큰 단위 비교로 고쳤다. ② v1 정확도는 분모가 "생성한 권수"라 누락이 감점되지 않았다. v2 A는 gold 보유 권수가 분모다.

**같은 v2 기준으로 채점한 결과** (`ai/evaluation/runs/v2-compare/REPORT.md`)

| 지표(%) | 중간발표 | RAG 보강 후 재생성 | + 검증기 보강(오프라인) |
|---|---:|---:|---:|
| **KOMA-Q** | 68.5 | **85.8** | 87.5 |
| A 값 정확도 | 51.6 | 71.1 | 71.1 |
| B RAG 규칙 준수율 | 76.3 | 96.1 | 99.0 |
| B 위반 0건 레코드 | 0.0 | 60.6 | 87.9 |
| C 생성/보류 판단 정확도 | 77.6 | 90.1 | 92.5 |
| C 과잉 생성률 ↓ | 17.8 | 13.9 | 8.7 |
| C 누락률 ↓ | 26.5 | 6.4 | 6.4 |

필드별 A: 245 41.9→80.4(책임표시 6.1→96.2), 300 13.6→59.1, 700 65.7→93.0, 056 28.8→45.5, 260 86.4→92.4, 041 33.3→100(n=3). 653은 26.2→24.8로 **소폭 하락**했다.

**감도 확인.** 중간발표 결과는 책임표시를 `245$c`에 넣어 A에서 0점을 받는다. 이를 값으로 인정하고(`$c`→`$d`) 구조 위반은 B에서만 감점해도 중간발표 A는 56.4, KOMA-Q는 70.1이다. 개선 폭은 유지된다.

**검증기 보강** (`backend/services/output_validator.py`, 테스트 5건 추가). RAG skip 규칙을 코드로 집행한다. LLM이 스스로 "보류 대상"이라고 설명하면서도 필드를 출력하던 사례들이다.

- 710: 역할어가 출판·발행이거나 이름이 `biblio.publisher`와 겹치면 제거 (재생성 결과 5권 6건)
- 246: 245 본표제와 구두점·공백만 다르면 제거 (5권)
- 546: "확인되지 않음"처럼 근거가 없다는 문장은 키워드가 있어도 제거 (2권)

"+ 검증기 보강" 열은 `ai/evaluation/revalidate_offline.py`로 저장된 LLM 출력에 현재 검증기를 다시 적용한 값이다. 원본과 비교해 위 13개 필드가 빠진 것 외에 달라진 필드가 없음을 확인했다. **실제 재생성 수치는 아니다.**

**남은 격차** (다음 개선 대상, 영향 큰 순)

1. 500 과잉 생성 19권. 책소개 요약을 일반주기로 쓴다. gold 500은 원저자명·공저자·감수 등 정해진 유형뿐이다. 500 RAG 규칙을 허용 유형 목록으로 좁혀야 한다(RAG 담당).
2. 245 부제(`$b`) 16권 모두 누락. API 표제에 부제가 없어 규칙 레이어가 나눌 근거가 없다. 부제가 있는 수집원이 필요하다.
3. 300 쪽수 1~5쪽 차이(19권 partial). API는 총 쪽수, gold는 마지막 번호 쪽을 쓴다.
4. 653 F1 0.25, 056 exact 42.4%, 490 누락 10권(gold 440 기준).

재현 명령

```sh
python3 ai/evaluation/revalidate_offline.py \
  --in-dir ai/evaluation/runs/after-rag/results \
  --out-dir ai/evaluation/runs/after-rag-revalidated/results
python3 ai/evaluation/evaluate_v2.py \
  --run mid-presentation=mid_result/eval_results \
  --run after-rag=ai/evaluation/runs/after-rag/results \
  --run after-rag+validator=ai/evaluation/runs/after-rag-revalidated/results \
  --out-dir ai/evaluation/runs/v2-compare
```

## 8. 아직 하지 않은 것

- 검증기 보강을 반영한 실제 33권 재생성(현재 "+ 검증기 보강" 열은 오프라인 재적용 값).
- 사서 직접 검수, 실제 도서 실물 대조, LAS 반입 검증.
- 프런트 연동 수정(배열/객체 계약 불일치, 목업 폴백, 검증·내보내기 미연결).
- 원격 push·PR 생성.
